# Stroke

full Stroke configuration.

```LuaU
Shell.Stroke({
	Thickness = 2,
	StrokeMode = "Border", -- If this is a Label then you can apply the stroke to the text itself ("Text")
	Color = Color3.fromRGB(255, 255, 255)
})(Frame.Ptr)
```